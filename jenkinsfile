pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'docker build -t python:1.2.0 .'
            }
            steps {
                sh 'docker run python:1.2.0'
            }
        }
    }
}